#include "mcts.h"

int main(void)
{
    //  mergeNode();
    learn();
    // Condition condition = initCondition();
    // char str[1341];
    // char str2[1343];
    // getBinaryFromCondition(str, condition);
    // sprintf(str2, "0b%s", str);
    // printf("%s\n", str2);
    return 0;
}